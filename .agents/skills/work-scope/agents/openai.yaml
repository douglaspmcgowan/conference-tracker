interface:
  display_name: "Work Scope"
  short_description: "Control scoped work and verified transitions"
  default_prompt: "Use $work-scope to inspect the active scope cell and choose the next verified transition. Queue findings with Capture-WorkDiscovery.ps1 or Add-ProjectIntake.ps1, report queued with the queue path, and leave the single drain command to the orchestrator."
